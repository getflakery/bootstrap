variables:
  - &vol '/mnt/nix:/mnt/nix'
steps:
  - test:
    image: nixos/nix
    commands:
      - echo 'experimental-features = flakes nix-command' >> /etc/nix/nix.conf
      - nix build --store 'local?store=/mnt/nix/store&state=/mnt/nix/state&log=/mnt/nix/log'  .#test -L
    volumes:
      - *vol

