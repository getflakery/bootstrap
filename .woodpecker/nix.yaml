variables:
  - &vol '/mnt/nix:/mnt/nix'
steps:
  - test:
    image: nixos/nix
    commands:
      - echo 'experimental-features = flakes nix-command' >> /etc/nix/nix.conf
      - nix build --real /mnt/nix --store /mnt/nix .#test -L
    volumes:
      - *vol

