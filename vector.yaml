# vector toml to read from stdin and write to stdout
sources:
  in:
    type: "stdin"

sinks:
  my_sink_id:
    type: http
    inputs:
      - in
    uri: http://flakery.dev/api/deployments/log/rebuild/e0895965-5bfa-4bb9-ab7f-68a55212976f
    encoding: 
      codec: json